{
  "$id": "https://proofable.me/schemas/wallet-risk.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Check a wallet for risk signals before allowing an action",
  "properties": {
    "chain": {
      "enum": [
        "eth",
        "base",
        "bsc",
        "pol",
        "opt",
        "arb",
        "sol",
        "ton",
        "sei",
        "sui",
        "btc",
        "stellar"
      ],
      "type": "string"
    },
    "chainId": {
      "minimum": 1,
      "type": "integer"
    },
    "provider": {
      "default": "webacy",
      "enum": [
        "webacy"
      ],
      "type": "string"
    },
    "walletAddress": {
      "anyOf": [
        {
          "format": "evm-address",
          "type": "string"
        },
        {
          "format": "solana-address",
          "type": "string"
        },
        {
          "format": "ton-address",
          "type": "string"
        },
        {
          "format": "sui-address",
          "type": "string"
        },
        {
          "format": "sei-address",
          "type": "string"
        },
        {
          "format": "bitcoin-address",
          "type": "string"
        },
        {
          "format": "stellar-address",
          "type": "string"
        }
      ]
    }
  },
  "required": [
    "walletAddress"
  ],
  "title": "wallet-risk",
  "type": "object"
}
