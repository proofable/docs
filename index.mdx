---
title: "The trust harness for people, apps, and agents."
sidebarTitle: "Docs"
description: "Docs for verification, gates, proofs, agents, and MCP."
"og:title": "Proofable Docs | The trust harness for people, apps, and agents."
"twitter:title": "Proofable Docs | The trust harness for people, apps, and agents."
icon: book
"og:image": "/images/og-default.png"
"twitter:image": "/images/og-default.png"
---

import ConnectHosts from '/snippets/connect-hosts.mdx';

Verify anything. Gate anything. Carry what's trusted. Prove what happened.

**Free to start. Pro is $19.99 a month.** [Pricing](/pricing)

## Any MCP client

Add `https://mcp.proofable.me/mcp` to an app, chat, or agent that speaks MCP, then sign in. Cursor, Claude, Codex, and VS Code are shortcuts.

<ConnectHosts />

Ask: `Show my Proofable profile and current proofs.`

## In your app

Verify a user or agent once. Check that result on your server.

```ts
import { ProofableClient, defineGate } from '@proofable/sdk';

const proofable = new ProofableClient();
const gate = defineGate([{ verifierId: 'proof-of-human' }]);
const result = await proofable.gateCheck({
  gate,
  subject: { accountId: user.accountAddress },
});

if (!result.satisfied) {
  // Show VerifyGate or Hosted Verify for result.missing, then check again.
  throw new Error('Verification required');
}

// Allow the action.
```

Your server allows or denies. Proofable returns the current result.

[Add Proofable to your product](/use-cases/build-a-platform)
